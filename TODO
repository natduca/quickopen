- Re-enable case sensitivity without any test breakage
- UI for truncated search

- Start daemon automatically by client; daemon reruns itself if out of date

- Boosts

- Manpages
- Vim and Emacs scripts

- FuzRe2 uses regexps but does not sanitize inputs
- FuzRe2 takes too long on long query strings

- Speed up empty-dir-query

- Report-bad-result / track non-first query/candidates/hit pairs
